# Coarse pre-flight allowlist for Trustissues. One regex per line; a line that
# matches is NOT treated as a leaked credential by mirror-secret-preflight.sh.
#
# Trustissues ships Shield, a PII and secret redactor. Testing a redactor requires
# feeding it strings that look exactly like live credentials, so the corpus and the
# shield tests are full of synthetic ones by design. The authoritative gate is the
# gitleaks scan over the filtered clone, which allowlists the same paths.
internal/shield/testdata/pii\.corpus
internal/shield/.*_test\.go
internal/handlers/.*_test\.go
# Placeholders in the operator-facing sample config and docs.
\.env\.example
docs/.*\.md
README\.md
SECURITY\.md
