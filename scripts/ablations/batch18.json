[
 {"id":"second-client-ip-derivation","mech":"client-ip","test":"TestForwardedHeaderCannotForgeTheAuditIP","file":"internal/handlers/service_secrets.go",
  "why":"the original bug: a second XFF reader with no trusted-peer gate let a direct caller choose its own audit IP",
  "old":"\treturn middleware.ClientIP(r)",
  "new":"\tif v := r.Header.Get(\"X-Forwarded-For\"); v != \"\" {\n\t\tparts := strings.Split(v, \",\")\n\t\tif last := strings.TrimSpace(parts[len(parts)-1]); last != \"\" {\n\t\t\treturn last\n\t\t}\n\t}\n\treturn r.RemoteAddr"},

 {"id":"vault-only-reaches-ai-gateway","mech":"authz","test":"TestVaultOnlyIsBlockedFromNonVaultSurfaces","file":"internal/middleware/auth.go",
  "why":"vault_only comes from the PUBLIC invite endpoint and could spend the team's provider key",
  "old":"\t\t\tif IsVaultOnly(r.Context()) {",
  "new":"\t\t\tif false && IsVaultOnly(r.Context()) {",
  "pkg":"./internal/middleware/"}
]
