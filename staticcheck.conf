# ST1005 (capitalized error strings) is disabled: rotation-provider errors
# deliberately start with the provider brand name ("Anthropic does not
# support programmatic key creation"), copied from dockyard's audited code.
checks = ["inherit", "-ST1005"]
